void testfunction{
  int i;
  for (i = 0; i < 42; i++) {} // Noncompliant

  for (i = 0; i < 42; i++);

  if (myVar == 4)
  {
    // Do nothing because of X and Y
  }

  switch (a) {
    case 1:
      break;
    default:
      break;
  }
}

void f()
{
  doSomething();
}

void emptyFunction()
{
  // This is intentionally empty
}